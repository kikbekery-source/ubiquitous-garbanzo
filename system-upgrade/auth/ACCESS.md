# 🔐 บันทึกการเข้าถึงระบบหลังบ้าน

> ตั้งเมื่อ: 1 ก.ค. 2026 · ตามคำสั่งโค้ชกิ๊ก

## รหัสเข้า /system/

| รายการ | ค่า |
|---|---|
| URL | https://coach-kik.com/system/ |
| Username | `kikkok` |
| Password | `2222` |
| วิธี | HTTP Basic Auth (.htaccess + .htpasswd) |

## สำหรับ agent / สคริปต์ที่ต้องยิง API หลัง login

```bash
curl -u kikkok:2222 "https://coach-kik.com/system/api/rooms-api.php"
```

## ⚠️ ข้อควรระวัง

1. รหัส `2222` สั้นมาก เดาง่าย — กันคนทั่วไป/บอทได้ แต่กันคนตั้งใจไม่ได้
   **แนะนำ:** เมื่อระบบนิ่งแล้วเปลี่ยนเป็นรหัสยาวขึ้น (แก้ได้ด้วยคำสั่ง
   `openssl passwd -apr1 <รหัสใหม่>` แล้วแทนค่าใน `.htpasswd`)
2. ไฟล์นี้อยู่ใน git — ถ้ารีโพนี้ถูกเปิด public เมื่อไหร่ ต้องเปลี่ยนรหัสทันที
3. อย่าเอารหัสไปใส่ในไฟล์ที่อยู่ใต้ `/system/` ที่เข้าถึงได้จากเว็บ (ยกเว้น `.htpasswd` ซึ่ง Apache ไม่ยอมเสิร์ฟอยู่แล้ว)
